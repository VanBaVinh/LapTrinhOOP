public class BorrowSlip {

    private Reader reader;
    private Book book;

    public BorrowSlip(Reader reader,
                      Book book) {

        this.reader = reader;
        this.book = book;
    }
}